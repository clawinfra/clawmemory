version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign

linters-settings:
  exclusions:
    rules:
      # Allow staticcheck nil-pointer warnings where we use t.Fatal
      - linters: [staticcheck]
        text: "SA5011"
