{
  "name": "@clawinfra/clawmemory-plugin",
  "version": "0.1.0",
  "description": "ClawMemory OpenClaw plugin — auto-capture and recall agent memory",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --coverage",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "keywords": ["clawmemory", "openclaw", "agent", "memory"],
  "license": "MIT"
}
